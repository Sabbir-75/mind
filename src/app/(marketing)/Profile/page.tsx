import React from 'react';

export const metadata = {
    title: "Mind || Profile",
}

const page = () => {
    return (
        <div>
           <h1>Profile</h1> 
        </div>
    );
};

export default page;