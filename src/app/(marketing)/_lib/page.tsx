import React from 'react';

const page = () => {
    return (
        <div>
            you do not show this page because this is a private page of folder
        </div>
    );
};

export default page;